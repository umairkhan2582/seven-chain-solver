version: "3.9"

services:
  solver:
    build: .
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json:ro
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
